This testcase returns a value from a register of a dead frame which contains a
value from which a sensitive value can be derived. This value should be erased
from the register before `secure_function` returns.
